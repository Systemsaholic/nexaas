/**
 * Weather Forecast skill task — registered in Trigger.dev.
 * Re-exports the skill's task definition so the worker discovers it.
 */
export { weatherForecast } from "../../skills/operations/weather-forecast/task.js";
